replicaCount: 1

image:
  repository: storefront-api
  tag: local
  pullPolicy: IfNotPresent

service:
  port: 8000

ingress:
  host: storefront.local
  path: /api

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
